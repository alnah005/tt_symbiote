# scripts/ttnn-pin.txt — tt_metal SOURCE BUILD config (no PyPI ttnn wheel; no global commit pin).
#
# ttnn is provided by a tt_metal SOURCE BUILD at $TT_METAL_HOME, NOT a PyPI wheel.
#
# There is deliberately NO single repo-wide tt-metal commit pin. Each model declares
# its OWN tt_metal commit in two places, and that per-model value is the source of truth:
#   - tests/<tree>/<model>/test_config.json              ("tt_metal_commit")
#   - src/tt_symbiote/models/<model>/modeling_<model>.py  (TT_METAL_COMMIT constant)
# Per-model drift is reported at test time by the autouse tt_metal_commit_check fixture
# (tests/conftest.py, non-blocking). bootstrap_venv.sh builds against whatever commit your
# $TT_METAL_HOME checkout happens to be on — it does not assert a global commit here.
#
# This file is sourced by scripts/bootstrap_venv.sh ONLY for the optional SFPI override.
# SFPI is auto-derived from $TT_METAL_HOME/tt_metal/sfpi-version (format: sfpi_version='X.Y.Z').
# Leave SFPI_REQUIRED empty to auto-derive; set it only to assert/override.
SFPI_REQUIRED=''
